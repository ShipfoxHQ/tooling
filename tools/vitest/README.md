# @shipfox/vitest

Lightweight wrapper to run Vitest with Shipfox defaults.

It should be used with other packages from [Shipfox](https://www.shipfox.io/).

- `vitest-run`: run tests with sensible defaults and tsconfig paths
- Integrates smoothly with other @shipfox tooling


