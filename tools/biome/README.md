# @shipfox/biome

Opinionated wrapper around Biome for formatting and linting across Shipfox repos. Ships platform binaries and a unified CLI.

It should be used with other packages from [Shipfox](https://www.shipfox.io/).

- Unified `biome` CLI for lint/format in CI and locally
- Consistent configuration baked into Shipfox toolchain
- Cross‑platform support via bundled Biome binaries


