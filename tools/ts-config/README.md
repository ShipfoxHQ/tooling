# @shipfox/ts-config

Shared TypeScript configuration used across Shipfox packages.

It should be used with other packages from [Shipfox](https://www.shipfox.io/).

- Provides a base `tsconfig.json` to extend
- Ensures consistent compiler options and path settings


