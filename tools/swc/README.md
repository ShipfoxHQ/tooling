# @shipfox/swc

Opinionated build wrapper around SWC for Shipfox TypeScript packages.

It should be used with other packages from [Shipfox](https://www.shipfox.io/).

- Single `swc` CLI for fast builds
- Path alias resolution via `tsc-alias` after emit
- Repo-friendly defaults for output structure


